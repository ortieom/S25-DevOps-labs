apiVersion: v1
kind: ConfigMap
metadata:
  name: test-config
  labels:
    {{- include "python-app.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Files.Get "files/config.json" | nindent 4 }} 
